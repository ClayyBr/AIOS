# Story 2.2: Gerenciador de Sessão por Telefone

## Status
Draft

## Executor Assignment
```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["eslint", "tsc", "vitest"]
```

## Story

**As a** agente IA,
**I want** manter contexto da conversa de cada cliente separadamente,
**so that** possa conduzir diálogos multi-turno sem misturar informações.

## Acceptance Criteria

1. Sessão identificada pelo número de telefone do cliente
2. Histórico de mensagens (até últimas 20) mantido em memória para contexto do Gemini
3. Timeout de sessão configurável (padrão: 30 minutos de inatividade)
4. Limpeza automática de sessões expiradas
5. Estado do carrinho de compras armazenado na sessão (itens, endereço, pagamento)

## Tasks / Subtasks

- [ ] Task 1: Criar tipos de sessão (AC: 1, 5)
  - [ ] Criar `src/types/session.types.ts`
  - [ ] Definir interface `Session`: phoneNumber, messageHistory, cart, deliveryAddress, paymentMethod, state, lastActivity, isHandoff
  - [ ] Definir enum `SessionState`: IDLE, ORDERING, AWAITING_ADDRESS, AWAITING_PAYMENT, AWAITING_PIX_PROOF, CONFIRMED, HANDOFF
  - [ ] Definir interface `CartItem`: menuItem, quantity, options, price
- [ ] Task 2: Criar SessionManager (AC: 1, 2, 3, 4)
  - [ ] Criar `src/services/session.manager.ts`
  - [ ] Usar `Map<string, Session>` indexado por número de telefone
  - [ ] Método `getOrCreateSession(phoneNumber)` → Session
  - [ ] Método `updateSession(phoneNumber, updates)` → void
  - [ ] Método `addMessage(phoneNumber, role, content)` → void (mantém max 20)
  - [ ] Método `clearExpiredSessions()` → void (remove sessões > SESSION_TIMEOUT_MINUTES)
  - [ ] `setInterval` para rodar limpeza a cada 5 minutos
- [ ] Task 3: Handoff helpers (AC: auxiliar para Story 4.2)
  - [ ] Método `setHandoff(phoneNumber, active)` → void
  - [ ] Método `isHandoff(phoneNumber)` → boolean
- [ ] Task 4: Testes unitários
  - [ ] Testar criação de sessão
  - [ ] Testar limite de 20 mensagens no histórico
  - [ ] Testar expiração de sessão
  - [ ] Testar handoff flag

## Dev Notes

### Constantes Relevantes
```typescript
const SESSION_TIMEOUT_MINUTES = env.SESSION_TIMEOUT_MINUTES || 30;
const MAX_HISTORY_LENGTH = 20;
const CLEANUP_INTERVAL_MS = 5 * 60 * 1000; // 5 minutos
```

### SessionState Flow
```
IDLE → ORDERING → AWAITING_ADDRESS → AWAITING_PAYMENT → AWAITING_PIX_PROOF → CONFIRMED
                                                     └→ CONFIRMED (se não Pix)
Qualquer estado → HANDOFF (se cliente pedir humano)
```

### Testing
- **Framework:** Vitest
- Testar criação/recuperação de sessões
- Testar rotação de histórico (> 20 mensagens)
- Testar timeout e limpeza automática
- Usar `vi.useFakeTimers()` para testar expiração

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-06 | 1.0 | Criação da story | @po (Pax) |

## Dev Agent Record
*Pendente*

## QA Results
*Pendente*
