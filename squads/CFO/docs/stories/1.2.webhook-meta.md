# Story 1.2: Webhook da Meta (Verificação + Recepção)

## Status
Draft

## Executor Assignment
```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["eslint", "tsc", "vitest"]
```

## Story

**As a** sistema,
**I want** receber mensagens do WhatsApp via webhook da Meta,
**so that** possa processar cada mensagem do cliente.

## Acceptance Criteria

1. Endpoint `GET /webhook` implementado com verificação de token (Meta Webhook Verification)
2. Endpoint `POST /webhook` implementado para receber mensagens (extracting: sender phone, message text, message type)
3. Validação de assinatura do payload (X-Hub-Signature-256) para segurança
4. Log estruturado de cada mensagem recebida (timestamp, remetente, tipo, preview)
5. Resposta `200 OK` imediata para a Meta (processamento assíncrono)

## Tasks / Subtasks

- [ ] Task 1: Implementar verificação de webhook GET (AC: 1)
  - [ ] Adicionar handler `GET /webhook` que verifica `hub.mode`, `hub.verify_token`, e retorna `hub.challenge`
  - [ ] Ler `META_VERIFY_TOKEN` do env
- [ ] Task 2: Implementar recepção de mensagens POST (AC: 2, 5)
  - [ ] Adicionar handler `POST /webhook` que extrai mensagens do payload da Meta
  - [ ] Criar tipos: `WebhookPayload`, `WebhookEntry`, `WebhookChange`, `MessageEvent` em `src/types/whatsapp.types.ts`
  - [ ] Extrair: `from` (telefone), `text.body` (mensagem), `type` (text/image/audio)
  - [ ] Responder `200 OK` imediatamente (antes do processamento)
- [ ] Task 3: Implementar validação de assinatura (AC: 3)
  - [ ] Criar `src/utils/signature.ts` com função `validateSignature(payload, signature, appSecret)`
  - [ ] Usar `crypto.createHmac('sha256', appSecret)` para validar `X-Hub-Signature-256`
  - [ ] Rejeitar requests com assinatura inválida (return 401)
- [ ] Task 4: Implementar logging estruturado (AC: 4)
  - [ ] Criar `src/utils/logger.ts` com instância pino configurada
  - [ ] Logar cada mensagem: `{ phoneNumber, messageType, timestamp }` (NUNCA logar conteúdo)
- [ ] Task 5: Testes unitários
  - [ ] Testar `validateSignature()` com assinaturas válidas e inválidas
  - [ ] Testar extração de dados do payload webhook

## Dev Notes

### Meta Webhook Verification (GET)
```
GET /webhook?hub.mode=subscribe&hub.verify_token=TOKEN&hub.challenge=CHALLENGE
→ Retorna: CHALLENGE (como texto plano)
```

### Meta Webhook Payload (POST) — Estrutura
```json
{
  "object": "whatsapp_business_account",
  "entry": [{
    "id": "WHATSAPP_BUSINESS_ACCOUNT_ID",
    "changes": [{
      "value": {
        "messaging_product": "whatsapp",
        "metadata": { "phone_number_id": "ID" },
        "messages": [{
          "from": "5512991650505",
          "id": "wamid.xxx",
          "timestamp": "1234567890",
          "type": "text",
          "text": { "body": "Olá, quero fazer um pedido" }
        }]
      },
      "field": "messages"
    }]
  }]
}
```

### Validação X-Hub-Signature-256
```typescript
import crypto from 'crypto';

function validateSignature(payload: string, signature: string, appSecret: string): boolean {
  const expectedSignature = 'sha256=' + crypto
    .createHmac('sha256', appSecret)
    .update(payload)
    .digest('hex');
  return crypto.timingSafeEqual(
    Buffer.from(signature),
    Buffer.from(expectedSignature)
  );
}
```

### Variáveis de Ambiente Necessárias
- `META_VERIFY_TOKEN` — Token customizado para verificação do webhook
- `META_APP_SECRET` — Secret da app Meta para validação de assinatura

### Testing
- **Framework:** Vitest
- Testar `validateSignature()` com assinaturas válidas/inválidas
- Testar extração de campos do payload
- Mock do request/response para testar handlers

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-06 | 1.0 | Criação da story | @po (Pax) |

## Dev Agent Record

### Agent Model Used
*Pendente*

### Completion Notes List
*Pendente*

### File List
*Pendente*

## QA Results
*Pendente*
