# Story 3.1: Integração Google Sheets — Leitura de Cardápio

## Status
Draft

## Executor Assignment
```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["eslint", "tsc", "vitest"]
```

## Story

**As a** agente IA,
**I want** consultar o cardápio e promoções do dia no Google Sheets,
**so that** possa informar o cliente sobre os itens disponíveis e preços.

## Acceptance Criteria

1. Service Account do Google configurado com acesso à planilha de cardápio
2. Função `getCardapio()` retornando itens com: nome, descrição, preço, categoria, disponibilidade
3. Função `getPromocoesAtivas()` retornando promoções do dia com preço promocional
4. Cache de 15 minutos para evitar chamadas excessivas à API do Sheets
5. System prompt do Gemini enriquecido com dados do cardápio e promoções

## Tasks / Subtasks

- [ ] Task 1: Criar tipos do cardápio (AC: 2, 3)
  - [ ] Criar `src/types/sheets.types.ts`
  - [ ] Interface `MenuItem`: name, description, price, category, available, options
  - [ ] Interface `Promocao`: name, originalPrice, promoPrice, description, validUntil
  - [ ] Interface `SheetConfig`: spreadsheetId, ranges
- [ ] Task 2: Criar SheetsService — Leitura (AC: 1, 2, 3, 4)
  - [ ] Criar `src/services/sheets.service.ts`
  - [ ] Inicializar Google Sheets API com Service Account (JSON base64 do env)
  - [ ] Método `getCardapio()` → lê aba "Cardápio" e mapeia para `MenuItem[]`
  - [ ] Método `getPromocoesAtivas()` → lê aba "Promoções" e filtra por data
  - [ ] Implementar cache com TTL de 15 minutos (variável `CARDAPIO_CACHE_MINUTES`)
  - [ ] Método `invalidateCache()` para forçar refresh
- [ ] Task 3: Enriquecer system prompt (AC: 5)
  - [ ] Atualizar `atendente.prompt.ts` para aceitar cardápio e promoções como parâmetro
  - [ ] Formatar lista de itens no prompt de forma legível para o Gemini
- [ ] Task 4: Testes unitários
  - [ ] Mockar googleapis SDK
  - [ ] Testar mapeamento de linhas da planilha para `MenuItem[]`
  - [ ] Testar cache (segunda chamada não faz request, expiração funciona)

## Dev Notes

### Estrutura da Planilha — Aba "Cardápio"
| Nome | Descrição | Preço | Categoria | Disponível | Opcionais |
|------|-----------|-------|-----------|------------|-----------|
| Dog Super | Hot dog completo com tudo | 25.90 | Hot Dogs | SIM | Maionese, Catchup, Mostarda |

### Estrutura da Planilha — Aba "Promoções"
| Nome | Preço Original | Preço Promo | Descrição | Válido Até |
|------|---------------|-------------|-----------|-----------|
| Combo Dog + Refri | 35.90 | 29.90 | Dog Super + Guaraná | 2026-03-15 |

### Autenticação Service Account
```typescript
import { google } from 'googleapis';

const credentials = JSON.parse(
  Buffer.from(env.GOOGLE_SHEETS_CREDENTIALS, 'base64').toString('utf-8')
);
const auth = new google.auth.GoogleAuth({
  credentials,
  scopes: ['https://www.googleapis.com/auth/spreadsheets'],
});
const sheets = google.sheets({ version: 'v4', auth });
```

### Testing
- **Framework:** Vitest
- Mockar `google.sheets.spreadsheets.values.get`
- Testar parsing de dados da planilha
- Testar mecanismo de cache com `vi.useFakeTimers()`

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-06 | 1.0 | Criação da story | @po (Pax) |

## Dev Agent Record
*Pendente*

## QA Results
*Pendente*
